# Term Explorer

A lightweight terminal file explorer written in Rust.
Navigate directories using arrow keys with a fast and minimal interface.
Built for simplicity and speed directly in your terminal.
